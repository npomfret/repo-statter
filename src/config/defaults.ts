import type { SimplifiedConfig } from './simplified-schema.js';
import { SIMPLIFIED_DEFAULTS } from './simplified-schema.js';

// Export the simplified defaults as the default configuration
export const DEFAULT_CONFIG: SimplifiedConfig = SIMPLIFIED_DEFAULTS;

